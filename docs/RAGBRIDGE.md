# RAGBridge

RAGBridge provides Retrieval-Augmented Generation for semantic memory.

## Features

- Stores knowledge in vector database
- Retrieves relevant context for queries
- Deduplicates and compresses context
- Resolves conflicts between sources

## Configuration

Edit config/rag.yaml to configure chunk size, overlap, and similarity threshold.
